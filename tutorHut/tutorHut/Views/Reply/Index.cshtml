@model tutorHut.Models.Profile
@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

@using (Html.BeginForm("Index", "Reply", FormMethod.Post, new { @class = "Form-Horizontal", role = "form", enctype = "multipart/form-data" }))
{
    @*<div>
       @Html.DisplayFor(model => model.ApplicationUser.Email)
    </div>
    <div>
        @Html.DisplayFor(model => model.ProfileFirstName)
    </div>
    <div>
        @Html.DisplayFor(model => model.ProfileLastName)
    </div>
    <div>
        @Html.DisplayFor(model => model.ProfilePhoneNumber)
    </div>*@


    <div class="form-group">
        <div class="col-md-12">
            @*<input type="submit" value="Accept" class="btn btn-default" name="accept" />*@
            <input class="btn btn-primary btn-lg" type="button" value="Accept" onclick="location.href='@Url.Action("Accept", "Reply")'" />
        </div>
    </div>

    <div class="form-group">
        <div class="col-md-12">
            @*<input type="submit" value="Deny" class="btn btn-default" name="deny"/>*@
            <input class="btn btn-primary btn-lg" type="button" value="Deny" onclick="location.href='@Url.Action("Deny", "Reply")'" />
        </div>
    </div>
    
}